[pytest]
pythonpath = .
testpaths  = tests/backend tests/ui
markers =
    integration: end-to-end tests that require live external services (Medium session)
    browser: Playwright browser tests that require a live server on port 8000
